# Configuration for notification manager.
# Wayland replacement for dunst
font=Hack 10

max-visible=3
#background-color=
#text-color=

# Set width of notification popups.
width=300
# Set maximium height of notification popups.
# Notifications whose text takes up less space are shrunk to fit.
height=100
# Some options set values that affect all four edges of a notification.
# These options can be specified in several different ways, depending on how much control over each edge is desired:
#   A single value will apply to all four edges.
#   Two values will set vertical and horizontal edges separately.
#   Three will set top, horizontal, and bottom edges separately.
#   Four will give each edge a separate value.
# When specifying multiple values, they should be comma-separated.
# For example, this would set the top margin to 10, left and right to 20, and bottom to five:
#    margin=10,20,5
# Set margin of each edge to the size specified by directional.
# See Directional Values for more information.
margin=5
# Set padding on each side to the size specified by directional
# See Directional Values for more information.
padding=5
# Set popup border size to px pixels.
border-size=3
border-color=#fb4934
background-color=#282828
text-color=#ebdbb2
# Set popup corner radius to px pixels.
border-radius=5

icons=1
max-icon-size=64

markup=1
# Applications may request an action to be associated with activating a notification.
# Disabling this will cause mako to ignore these requests.
actions=1

# Set notification format string to format.
# See Format Specifiers for more information.
# To change this for grouped notifications, set it within a grouped criteria.
# The format of the message.  Possible variables are:
#   %a  appname
#   %s  summary
#   %b  body
#   %g	Number of notifications in the current group
# Markup is allowed
# format="<b>%s</b>\n%b"
# format="<b>%a:</b> %s\n%b"
format=<b>%s</b>\n%b
#Default when grouped: (%g) <b>%s</b>\n%b

default-timeout=5000
ignore-timeout=0

[app-name=Spotify]
font=Hack 10
background-color=#14643bA0
format="<b>Now Playing:</b>\n%s\n%b"
text-color=#FFFFFF
border-size=0
ignore-timeout=1
default-timeout=3000
border-radius=15

[app-name=battery_check]
font=Hack 14
background-color=#282828
text-color=#fabd2f
border-color=#fb4934
